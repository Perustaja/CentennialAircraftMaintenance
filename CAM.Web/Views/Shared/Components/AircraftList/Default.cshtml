@model CAM.Web.ViewModels.Shared.AircraftListViewModel
    @for (int i = 0; i < Model.Ids.Count - 1; i++)
    {
        <option value="@Model.Ids[i]" selected="@Model.Selected[i]">@(Html.DisplayFor(m => m.Ids[i]))</option>
    }