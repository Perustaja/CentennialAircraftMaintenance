@model CAM.Web.ViewModels.Shared.WorkStatusListViewModel
    @for (int i = 0; i < Model.Descriptions.Count; i++)
    {
        <option value="@Model.Descriptions[i].ToLower()" selected="@Model.Selected[i]">@(Html.DisplayFor(m => m.Descriptions[i]))</option>
    }